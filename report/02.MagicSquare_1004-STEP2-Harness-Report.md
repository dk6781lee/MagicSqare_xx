# MagicSquare_1004 — STEP 2 보고서 (ECB + Dual-Track TDD Harness)

- **프로젝트:** 4×4 마방진 (ECB 실습)
- **상태:** Harness 골격 **완료** (구현·테스트 본문 없음)
- **작성일:** 2026-06-04
- **연계:** `report/01.*`, `docs/PRD.md`, `Prompring/02.MagicSquare_1004-STEP2-Transcript-Export.md`

---

## 1. 세션 목표

STEP 1(Mom Test·문제 정의) 이후, **Red → Green → Refactor** 루프를 돌릴 수 있는 **최소 Harness**를 ECB 3계층 + Dual-Track 테스트 디렉터리로 고정한다.

| 포함 | 제외 |
|------|------|
| `pyproject.toml` (pytest만) | Rule / Command 구현 |
| `src/{entity,control,boundary}/` | `test_*.py` 본문 |
| `tests/{entity,control,boundary}/` | Boundary UI |
| 동작 확인 방법 문서화 | Report/01 수정 |

---

## 2. 생성 산출물

### 2.1 디렉터리

```
MagicSqare_xx/
├── pyproject.toml
├── src/
│   ├── entity/__init__.py
│   ├── control/__init__.py
│   └── boundary/__init__.py
└── tests/
    ├── entity/__init__.py
    ├── control/__init__.py
    └── boundary/__init__.py
```

### 2.2 `pyproject.toml` 요약

| 항목 | 값 |
|------|-----|
| 패키지명 | `magicsquare-xx` v0.1.0 |
| Python | `>=3.10` |
| dev 의존성 | `pytest>=8.0`만 |
| 패키지 검색 | `src/` |
| pytest | `testpaths=tests`, `pythonpath=src` |

### 2.3 Dual-Track 매핑

| Track | 소스 | 테스트 | 1차 TDD 범위 (PRD) |
|-------|------|--------|---------------------|
| Entity | `src/entity/` | `tests/entity/` | 후속 (`MagicSquare`, `Cell`) |
| Control | `src/control/` | `tests/control/` | **우선** (`SquareValidator`, C1–C3) |
| Boundary | `src/boundary/` | `tests/boundary/` | 후속 (`GridUI` 등) |

---

## 3. 동작 확인

프로젝트 루트에서:

```powershell
pip install -e ".[dev]"
pytest
python -c "import entity, control, boundary; print('ok')"
```

**골격만 있을 때 기대:**

| 명령 | 기대 |
|------|------|
| `pytest` | `collected 0 items` (exit code 5) — 정상 |
| import 확인 | `ok` 출력 |

---

## 4. STEP 1 연계 (변경 없음)

- Mom Test 진짜 문제·증거 3줄 → `report/01.*` 그대로
- P0 테스트 (`CanClaimComplete`, 대각선 누락 SC2) → **다음 세션** `tests/control/` Red부터

---

## 5. 마일스톤

| 단계 | 상태 |
|------|------|
| STEP 1 — Mom Test · 문제 정의 | ✅ |
| STEP 2 — ECB Harness 골격 | ✅ |
| Q2 인터뷰 반영 | ⬜ |
| P0 Red 테스트 (`ValidateAllLines`, `CanClaimComplete`) | ⬜ |
| Control Green 구현 | ⬜ |

---

## 6. 다음 단계

1. `tests/control/test_validator.py` — PRD §7 P0 Red (대각선 1개 실패 → `CanClaimComplete = false`)
2. `src/control/square_validator.py` — Green (C1–C3)
3. Q2 답변 반영 시 테스트 시나리오 추가

---

## 7. 관련 문서

- `report/01.MagicSquare_ProblemDefinition_Report.md`
- `report/01.MagicSquare_1004-STEP1-MomTest-Report.md`
- `docs/PRD.md`
- `Prompring/02.MagicSquare_1004-STEP2-Transcript-Export.md`
